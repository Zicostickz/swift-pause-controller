[project]
name = "swift-pause-controller"
authors = ["Swift Labs"]
description = "A decentralized asset pausability and control mechanism for Stacks blockchain"
telemetry = false
requirements = []

[contracts.pause-manager]
path = "contracts/pause-manager.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
